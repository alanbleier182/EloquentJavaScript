// This function calculates the minimum between 2 numbers

function min(a,b) {
  let minimum = a;
  
  if(b < a) {
    minimum = b;
  }
  return minimum
}

console.log(min(-1,1)); // Prints "-1" (the minimun)
